After using the program "aruco_mapper_fromvideo" from Aruco Markermap library, the program will give a yml that has the xyz 
from each corner of all ID, my program is able to calculate the distance between all markers regarding one marker.

To compile the program put the CMakeLists.txt in the same directory of FileStorage.cpp, in terminal type "cmake ." and "make"
